server:
  port: ${SERVER_PORT:8080}

spring:
  application:
    name: simpleweb

app:
  name: SimpleWeb
  version: @project.version@
  environment: ${APP_ENVIRONMENT:local}

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: never
